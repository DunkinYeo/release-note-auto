# Copy this file to: config/release_config.yaml and edit values as needed.

# If tag is empty, tool auto-detects latest tag and previous tag.
tag: ""

# Default date string to print (if empty, uses today's date)
date: ""

# Optional manual overrides (if you don't want tag parsing)
platform: ""   # iOS / Android
version: ""    # 2.1.5

# Release note content (used in addition to parsed commits)
new_functionalities:
  - "Set/Get Sample Rate function added."

enhancements:
  - "Improved BLE stability."
  - "Error Handling, Timeout improvements."
  - "General Improvements - Bug fix."
  - "SDK Doc update."

previous_versions:
  - version: "v2.1.4"
    description: "Update SDK_EX_Guide.md : content"
  - version: "v2.1.3"
    description: "Update Error handling fix code SDK file"

contact:
  email: "spatch.cardio@wellysis.com"
  phone: "+1 281 965 0330"
  address: "2450 Holcombe Blvd, Houston, TX 77021"

slack:
  enabled: false
  webhook_url: ""   # paste incoming webhook URL here
  channel_note: "UAT"  # optional label included in message
